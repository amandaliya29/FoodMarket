import {StyleSheet, Text, View} from 'react-native';
import React from 'react';

const AdminNewOrder = () => {
  return (
    <View style={{flex: 1, backgroundColor: '#fff'}}>
      <Text>AdminNewOrder</Text>
    </View>
  );
};

export default AdminNewOrder;

const styles = StyleSheet.create({});
